//
// Created by Ignacio on 14/10/2024.
//

#include "internal_std_isOnGround.h"


int internal_std_isOnGround(client * c){

    if(NULL)
    {

    }
    const int pointsCountsToCheckOnGrid = 6;
    for(int i = 0; i < pointsCountsToCheckOnGrid; i++)
        for(int j = 0; j < pointsCountsToCheckOnGrid; j++){
            //
        }
}